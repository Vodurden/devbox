#
# .profile
#

# Source any additional profile scripts in ~/.profile.d
if [[ -d "${HOME}/.profile.d" ]]; then
    for script in ${HOME}/.profile.d/*.sh ; do
        if [ -r "${script}" ]; then
            source ${script}
        fi
    done
fi;
